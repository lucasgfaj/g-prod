<!DOCTYPE html>
<html lang="pt-br">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%= stylesheet_link_tag "global", media: "all" %>
    <%= stylesheet_link_tag "index", media: "all" %> <!-- Inclui apenas a folha de estilo relevante -->
    <%= javascript_include_tag "application" %>
    <title>G-Prod</title>
    <style>
        .header-icon {
            width: 40px; /* Ajuste o tamanho conforme necessário */
            height: auto; /* Mantém a proporção */
            margin-right: 10px; /* Espaço entre o ícone e o texto */
        }
        /* Adicione outras regras de estilo conforme necessário */
    </style>
</head>
<body>
    <!-- Header -->
    <header>
        <nav>
            <ul>
                <li><img src="/icon.svg" alt="G-Prod Icon" class="header-icon"></li> <!-- Adicionando o ícone -->
                <li><%= link_to 'Home', root_path %></li>
                <li><%= link_to 'Mostrar Produtos', produtos_path %></li>
                <li><%= link_to 'Novo Produto', new_produto_path %></li>
            </ul>
        </nav>
    </header>

    <!-- Main Content -->
    <main>
        <h1>Lista de Produtos</h1>
        <table>
            <thead>
                <tr>
                    <th>Nome</th>
                    <th>Descrição</th>
                    <th>Preço</th>
                    <th>Quantidade</th>
                    <th colspan="3">Ações</th>
                </tr>
            </thead>

            <tbody>
                <% @produtos.each do |produto| %>
                    <tr>
                        <td><%= produto.nome %></td>
                        <td><%= produto.descricao %></td>
                        <td><%= produto.preco %></td>
                        <td><%= produto.quantidade %></td>
                        <td><%= link_to 'Mostrar', produto, class: 'btn-show' %></td>
                        <td><%= link_to 'Editar', edit_produto_path(produto), class: 'btn-edit' %></td>
                        <td><%= link_to 'Deletar', produto, method: :delete, data: { confirm: 'Tem certeza?' }, class: 'btn-delete' %></td>
                    </tr>
                <% end %>
            </tbody>
        </table>
    </main>

    <!-- Footer -->
    <footer>
        <p>&copy; 2024 G-Prod. Todos os direitos reservados.</p>
    </footer>
</body>
</html>
